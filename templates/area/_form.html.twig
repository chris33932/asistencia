

<div class"container" style="widht: 75%">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary" style="margin: 20px">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</div>
